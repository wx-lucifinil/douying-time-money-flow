{
  "name": "douying-time-money-flow",
  "version": "0.1.0",
  "private": true,
  "description": "A-share sector fund-flow vertical chart preview page.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "record:morning": "node scripts/record-flow.mjs --session=morning",
    "record:close": "node scripts/record-flow.mjs --session=close"
  },
  "engines": {
    "node": ">=18"
  }
}
